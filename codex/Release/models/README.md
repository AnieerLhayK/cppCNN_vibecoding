# Model Placeholder

正式发布模型应命名为：

```text
gtsrb_subset10.bin
```

该文件对应 `labels.txt` 中的 10 个类别，使用项目自定义 `CPPCNN1` 二进制格式。

当前处于项目搭建阶段，按照要求没有进行正式训练，因此这里只保留目录和说明，不提供空权重、随机权重或伪造模型。

模型文件属于生成产物，不提交到 Git。训练完成后，由发布负责人将模型复制到本目录，再交付完整演示包。
