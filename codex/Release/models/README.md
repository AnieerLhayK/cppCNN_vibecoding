# Model Placeholder

正式发布模型应命名为：

```text
gtsrb_v2_subset10.bin
```

该文件对应 `labels.txt` 中的 10 个类别，使用项目自定义 `CPPCNN1` 二进制格式。

本地 Release 包现已放入经过实际训练和测试的模型，不是空权重或随机参数。该二进制文件仍被 Git 忽略，不会进入源码仓库。

模型文件属于生成产物，不提交到 Git。训练完成后，由发布负责人将模型复制到本目录，再交付完整演示包。
