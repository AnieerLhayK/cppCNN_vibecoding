# CNN Project Boundary

This Claude Code project is the Git repository containing this rule.

- Default writes must stay inside this repository.
- Never create or modify files in `D:\AI\workspace` during CNN work.
- Use `claude/` for the Python/PyTorch prototype and `codex/` for the C++ implementation.
- If a request appears to target another repository, stop and report the current Git root before acting.
